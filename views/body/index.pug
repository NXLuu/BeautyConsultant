doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    meta(http-equiv='X-UA-Compatible' content='IE=edge')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    link(rel='stylesheet' href='/css/skin.css')
    link(rel='stylesheet' href='/test.css')


    title Beauty Consultant
  body
    .container

      form(method="post" action= action)
        if error
          .alert.alert-danger(role='alert')
            | #{mess}
            a.alert-link(href='/result' style="margin-left: 10px") 
              | bỏ qua
       
        .card
            h2
                | Nhập số đo ngực (10 - 200) cm
            input(id='bust' type="number" name='bust' min="10" max='200')
        .card
            h2
                | Nhập số đo mông (10 - 250) cm
            input(id='hip' type="number" name='hip' min="10" max='250')
        .card
            h2
                | Nhập vào số đo eo ? (20 - 400) cm
            input(id='waist' type="number" name='waist' min="20" max='400')
        button.submit-btn(value='submit') Submit

